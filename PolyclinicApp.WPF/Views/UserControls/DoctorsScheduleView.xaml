<UserControl x:Class="PolyclinicApp.WPF.Views.UserControls.DoctorsScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PolyclinicApp.WPF.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ColumnWidth="Auto" ItemsSource="{Binding ScheduleTable}" FontSize="10" IsReadOnly="True" CanUserResizeColumns="False" CanUserResizeRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Врач" Binding="{Binding Doctor.FullName}"/>
                <DataGridTextColumn Header="ПН" Binding="{Binding Monday}"/>
                <DataGridTextColumn Header="ВТ" Binding="{Binding Tuesday}"/>
                <DataGridTextColumn Header="СР" Binding="{Binding Wednesday}"/>
                <DataGridTextColumn Header="ЧТ" Binding="{Binding Thursday}"/>
                <DataGridTextColumn Header="ПТ" Binding="{Binding Friday}"/>
                <DataGridTextColumn Header="СБ" Binding="{Binding Saturday}"/>
                <DataGridTextColumn Header="ВС" Binding="{Binding Sunday}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
