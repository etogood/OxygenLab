<UserControl x:Class="PolyclinicApp.WPF.Views.UserControls.InformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PolyclinicApp.WPF.Views.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <DataGrid AutoGenerateColumns="False" ColumnWidth="Auto" ItemsSource="{Binding OrdersTable}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Пациент" Binding="{Binding Patient.FullName}"/>
                <DataGridTextColumn Header="Лечащий врач" Binding="{Binding Doctor.FullName}"/>
                <DataGridTextColumn Header="Жалоба" Binding="{Binding Disease}"/>
                <DataGridTextColumn Header="Время приёма" Binding="{Binding DateOfDiagnosis}"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>